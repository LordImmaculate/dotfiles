return {
  "folke/tokyonight.nvim",
  lazy = false,
  priority = 1000,
  opts = {
    -- Change the style if you want, `night` is the default
    style = "storm",
    -- To enable transparent background
    transparent = true,
  },
}
